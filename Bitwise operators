// To check the no. of 1s in a given binary number//
#include<stdio.h>
int main()
{
        int num, count=0;
        printf("Enter the value: ");
        scanf("%d", &num);
        while(num)
        {
                count+=(num&1);
                num>>=1;
        }
        printf("%d\n", count);
}



