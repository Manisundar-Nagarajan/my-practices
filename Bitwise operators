// To set the perticular bit of a numer//
#include<stdio.h>
int main()
{
        int num, pos,x;
        printf("Enter the number: ");
        scanf("%d", &num);
        printf("Enter the position: ");
        scanf("%d", &pos);
        x=1<<pos;
        printf("%d\n", (num|x));
}

