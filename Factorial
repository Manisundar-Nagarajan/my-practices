#include<stdio.h>
int main()
{
int num, fact=1;
printf("Enter the value: ");
scanf("%d", &num);
while(num!=0)
{
fact=fact*num;
num--;
}
printf("The factorial of the given number is: %d\n", fact);
return 0;
}
