/*Revere the digits of a given number*/
#include<stdio.h>
int main()
{
int num, rev;
printf("Enter the number: ");
scanf("%d", &num);
while(num!=0)
{
rev=num%10;
num=num/10;
printf("%d", rev);
}
printf("\n");
return 0;
}
